[package]
name = "claude-hooks"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1"
regex = "1"
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
